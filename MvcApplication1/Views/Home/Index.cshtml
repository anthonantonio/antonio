@model IEnumerable<MvcApplication1.Models.Accounts>

  @Html.ActionLink("Add","Add")
  <br />

  @foreach(var item in Model)
  {
      @Html.DisplayFor(asdas => item.Name)
       @Html.DisplayFor(asdas => item.Id)
        @Html.DisplayFor(asdas => item.Age)
        <br />
  }